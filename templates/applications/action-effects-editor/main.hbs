<section class="tab {{tab.id}} {{tab.cssClass}}" data-tab="{{tab.id}}" data-group="{{tab.group}}">
	<label class="form-group slim">
		<span>{{localize "ACTION.FIELDS.effects.symbolType.label"}}</span>

		<span class="form-fields">
			<select name="symbol" {{#if symbol}}disabled{{/if}}>
				{{selectOptions symbols selected=symbol labelAttr="name" localize=true}}
			</select>
		</span>
	</label>

	{{formGroup fields.symbolAmount
				value=effect.symbolAmount
				classes="slim"
				placeholder=(localize "ACTION.FIELDS.effects.symbolAmount.label")
				localize=true}}

	{{formGroup fields.description
				value=effect.description
				enriched=enrichedDescription
				toggled=true
				localize=true}}
</section>