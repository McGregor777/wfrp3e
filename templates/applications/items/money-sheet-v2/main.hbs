<section class="tab {{tab.id}} {{tab.cssClass}}" data-tab="{{tab.id}}" data-group="{{tab.group}}">
	{{formGroup fields.value value=system.value localize=true}}

	{{formGroup fields.quantity value=system.quantity localize=true}}

	{{formGroup fields.description value=system.description localize=true}}
</section>