<section class="tab {{tab.id}} {{tab.cssClass}}" data-tab="{{tab.id}}" data-group="{{tab.group}}">
	{{formGroup fields.characteristic value=system.characteristic localize=true}}

	{{formGroup fields.advanced value=system.advanced localize=true}}

	{{formGroup fields.description value=system.description localize=true}}

	{{#if parent}}
		{{formGroup fields.specialisations value=system.specialisations localize=true}}
	{{/if}}
</section>