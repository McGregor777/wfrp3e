<div class="draggable ability item card" data-item-id="{{item._id}}">
	{{#if @root.editable}}
		<a class="name" data-action="editItem" data-tooltip="{{localize 'ACTOR.ACTIONS.editItem'}}">
			{{item.name}}
		</a>
	{{else}}
		<span>{{item.name}}</span>
	{{/if}}

	<div class="description">
		{{{lookup @root.enrichment item.uuid }}}
	</div>

	<div class="bottom-line">
		{{#if @root.editable}}
			<a class="recharge-token prevent-context"
			   data-action="adjustRechargeTokens"
			   data-tooltip="{{localize 'ACTOR.ACTIONS.adjustRechargeTokens'}}">
				<img src="systems/wfrp3e/assets/images/token-grey.webp"
					 alt="{{localize 'ABILITY.FIELDS.rechargeTokens.label'}}" />

				<span class="recharge-token-counter">
					{{{talent.system.rechargeTokens}}}
				</span>
			</a>

			{{#each item.effects as |effect ef|}}
				{{#if (and (eq effect.system.macro.type "manual") effect.system.macro.script (not effect.isSuppressed))}}
					<a data-action="useItem"
					   data-effect-uuid="{{effect.uuid}}"
					   data-tooltip="{{localize 'ABILITY.ACTIONS.trigger'}}" >
						{{localize "ABILITY.ACTIONS.trigger"}}
					</a>
				{{/if}}
			{{/each}}

			<div>
				<a data-action="editItem" data-tooltip="{{localize 'ACTOR.ACTIONS.editItem'}}">
					<span class="fas fa-pen-to-square"></span>
				</a>

				<a data-action="deleteItem" data-tooltip="{{localize 'Delete'}}">
					<span class="fas fa-trash"></span>
				</a>
			</div>
		{{else}}
			<span class="recharge-token" data-tooltip="{{localize 'ABILITY.FIELDS.rechargeTokens.label'}}">
				<img src="systems/wfrp3e/assets/images/token-grey.webp"
					 alt="{{localize 'ABILITY.FIELDS.rechargeTokens.label'}}" />

				<span class="recharge-token-counter">
					{{{talent.system.rechargeTokens}}}
				</span>
			</span>
		{{/if}}
	</div>
</div>
